---

epel_repo_rpm: "epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm"
epel_repo_url: "http://download.fedoraproject.org/pub/epel/{{epel_repo_rpm}}"
epel_repo_gpg_key_url: "/etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-{{ ansible_distribution_major_version }}"
